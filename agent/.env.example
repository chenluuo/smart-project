# 环境变量模板：复制为 .env 并按环境填写（.env 不入库）
# 敏感配置只走环境变量，config.yaml 中对应位置为 ${VAR} 占位

LLM_API_KEY=sk-xxx
REDIS_PASSWORD=
MILVUS_TOKEN=
MINIO_SECRET_KEY=minioadmin
JWT_SECRET=change-me-same-as-go
GO_INTERNAL_KEY=change-me-shared
